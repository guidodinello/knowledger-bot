make a command to request features/improvements/ideas

ill need to refine how to process those, if just store them in a file or something else. im thinking that ultimately i want something like this .md file inside the docs/improvements folder but how do i reach that from the bot input? 
maybe integration con github? open an issue? open a PR? ill need to refine those improvement ideas with claude code later to generate the implementation plan and grill the feature requirements from me.